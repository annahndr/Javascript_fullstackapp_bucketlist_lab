const Request = require('../helpers/request.js');
const PubSub = require('../helpers/pub_sub.js');

const Item = function (url) { //URL is passed in from item creation in app.js
  this.url = url
}


Item.prototype.getData = function () {
  const 
};
